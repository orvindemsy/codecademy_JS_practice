const groceries = ['brown sugar', 'salt', 'cranberries', 'walnuts'];

//sum(2,3);

//console.log(sum(2));

const list = (a, b) => {
  a.forEach(b => console.log (' - ' + b));
}

list(groceries, item);
